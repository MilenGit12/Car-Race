let cars = Math.locate.group_module.cars(group_module = 4, draw_modules("./modules/Car_modules/tinker.obj", "./modules/Car_modules/obj.mtl"));
let road = Math.locate.road(draw_modules("./modules/7qz4e7jqw7wg-RaceTrack/Race Track", setCenter()));
let coins = Math.locate.module.coins(module = 2, draw_modules("./modules/Coins/tinker.obj", "./modules/Coins/obj.mtl"));
let finish_sign = Math.locate.group_module.finish_sign(group_module = 4, draw_modules("./modules/Finish/tinker.obj", "./modules/Finish/obj.mtl"), setWheresFinish());
let module = module.render()
let style = module.style[i]
let i = style.module(style / 1/2 / style/render/bar)
let color = {Range};
let x = Range;
let y = Range;

const draw_modules = module.render((module) => {
    let x = module.render(module.Math(x))
    let y = module.render(module.Math(y))
    module.draw = [
        lines = x,y,
        lines_copy = x,y,
        render.line = 1/2
        render.lines_copy = 1/2
    ]
})

if (color = 3.75) {
    color.Range
    Range++;
}else if(color = 5.00) {
    color.render()
    Range--;
}
if(draw_modules = module.render) {
    module.render = draw_modules(module.draw)
}
for (let car_module = 0; car_module < array.length; car_module++) {
    const car_group = array[car_module];
}
var Finish_line = finish_sign[toCar_module]
var create_route_on_hud = finish_sign[toCar_module]
