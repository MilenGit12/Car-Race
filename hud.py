import modules;
import modules_.hud.png;
import hud.png;
import route;
import road.module
import size;
import lines;

def center(x, y):
    x = 50
    y = 50
    if x is 50 in range():
        range.center == center()
        center(range)
        callable.x(range.x)
    if y is 50 in range():
        range.center == center()
        center(range)
        callable.y(range.y)
    if center is 50 in x(x = 50):
        range.center()
    if center is 50 in y(y = 50):
        range.center()

    center == x == y
def hud(map):
    hud.route()
    modules.route()
    if hud in route():
        route == route.road == hud.road()
        road.route == hud(route.routes()).show
    for route in hud(size * size, lines * lines):
        route in hud(route.road(road.lines))
        route.lines == road.lines == center(hud)