import car.module;
import modulefinder;
import modules;
import road.module;

x = range.count(1-50)
y = range.count(1-50)

def moving():
    moving.moves() == ()
def module():
    if car in road is moving():
        moving.car == x, y